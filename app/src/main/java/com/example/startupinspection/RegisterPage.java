package com.example.startupinspection;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

/**
 * Unused due to security reasons if posted on Google Play Store
 * Template for registering new users and adding user profiles to the procedure
 * Should not be needed, all user profiles should be added using Authentication on Firebase console
 *
 * @author Kyle Kline
 */

public class RegisterPage extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_register_page);
    }
}